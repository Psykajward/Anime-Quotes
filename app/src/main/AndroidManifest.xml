<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/image2"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/image1"
        android:supportsRtl="true"
        android:theme="@style/Theme.SecondAttempt"
        tools:targetApi="31">
        <activity android:name=".QuotesActivity" />
        <activity
            android:name=".SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Quote1Activity" />
        <activity android:name=".Quote2Activity" />
        <activity android:name=".Quote3Activity" />
        <activity android:name=".Quote4Activity" />
        <activity android:name=".Quote5Activity" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>